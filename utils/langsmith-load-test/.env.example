LANGSMITH_TRACING=true
LANGSMITH_ENDPOINT=https://langsmith.example.com/api/v1
LANGSMITH_API_KEY=replace-with-your-api-key
LANGSMITH_PROJECT=load-test

# Set only when the API key can access multiple workspaces.
# Find this in the target workspace under Settings > General.
LANGSMITH_WORKSPACE_ID=
