name: CodeQL

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  schedule:
    - cron: "30 1 * * 0"  # weekly on Sunday

permissions:
  actions: read
  contents: read
  security-events: write

# No CodeQL-supported languages in this repo yet (Terraform/HCL is not supported).
# Uncomment and populate the matrix as new assets are introduced
# (e.g. "python", "javascript-typescript", "go").
#
# jobs:
#   analyze:
#     name: Analyze (${{ matrix.language }})
#     runs-on: ubuntu-latest
#     strategy:
#       fail-fast: false
#       matrix:
#         language: ["python", "javascript-typescript"]
#
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
#
#       - name: Initialize CodeQL
#         uses: github/codeql-action/init@v3
#         with:
#           languages: ${{ matrix.language }}
#           queries: security-extended
#
#       - name: Autobuild
#         uses: github/codeql-action/autobuild@v3
#
#       - name: Perform CodeQL Analysis
#         uses: github/codeql-action/analyze@v3
#         with:
#           category: "/language:${{ matrix.language }}"
jobs:
  noop:
    name: CodeQL (no supported languages)
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No CodeQL-supported languages in this repo. See comments above."'
